import type { MetadataRoute } from 'next';import { conditions } from '@/data/conditions';import { points } from '@/data/points';import { meridians } from '@/data/meridians';
export default function sitemap(): MetadataRoute.Sitemap { const base='https://acupoint-atlas.vercel.app'; const paths=['','/conditions','/points','/meridians','/acupressure-guide','/safety','/disclaimer','/about','/search',...conditions.map(c=>`/conditions/${c.slug}`),...points.map(p=>`/points/${p.slug}`),...meridians.map(m=>`/meridians/${m.slug}`)]; return paths.map(p=>({url:base+p,lastModified:new Date()})); }
