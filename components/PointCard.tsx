import Link from 'next/link';import Tag from './Tag';import type { Point } from '@/data/points';
export default function PointCard({point}:{point:Point}){return <article className="flex h-full flex-col rounded-2xl border border-pine/10 bg-white p-5 shadow-sm transition hover:-translate-y-1 hover:shadow-soft"><h3 className="text-xl font-bold text-pine">{point.name} {point.code}</h3><p className="text-sm text-ink/60">{point.pinyin} · {point.meridianName} · {point.bodyRegion}</p><p className="mt-3 flex-1 leading-7 text-ink/75">{point.summary}</p><div className="mt-4 flex flex-wrap gap-2">{point.tags.slice(0,5).map(t=><Tag key={t}>{t}</Tag>)}</div><Link href={`/points/${point.slug}`} className="mt-5 inline-flex min-h-11 items-center justify-center rounded-full bg-pine px-4 py-2 text-sm font-semibold text-white">查看详情</Link></article>}
