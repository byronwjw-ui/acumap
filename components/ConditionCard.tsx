import Link from 'next/link';import Tag from './Tag';import type { Condition } from '@/data/conditions';
export default function ConditionCard({condition}:{condition:Condition}){return <article className="flex h-full flex-col rounded-2xl border border-pine/10 bg-white p-5 shadow-sm"><h3 className="text-xl font-bold text-pine">{condition.shortTitle}</h3><p className="mt-3 flex-1 leading-7 text-ink/75">{condition.summary}</p><p className="mt-3 text-sm font-semibold text-gold">相关穴位 {condition.relatedPointSlugs.length} 个</p><div className="mt-4 flex flex-wrap gap-2">{condition.tags.slice(0,4).map(t=><Tag key={t}>{t}</Tag>)}</div><Link href={`/conditions/${condition.slug}`} className="mt-5 inline-flex min-h-11 items-center justify-center rounded-full bg-pine px-4 py-2 text-sm font-semibold text-white">查看详情</Link></article>}
