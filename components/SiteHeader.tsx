'use client';
import Link from 'next/link';import { useState } from 'react';import { Menu, X } from 'lucide-react';import { navItems } from '@/data/site';import SearchBox from './SearchBox';
export default function SiteHeader(){const[open,setOpen]=useState(false);return <header className="sticky top-0 z-40 border-b border-pine/10 bg-rice/90 backdrop-blur"><div className="mx-auto flex max-w-7xl items-center gap-4 px-4 py-3"><Link href="/" className="font-bold text-pine">AcuPoint Atlas <span className="hidden sm:inline">穴位图谱</span></Link><nav className="ml-auto hidden items-center gap-5 text-sm font-medium md:flex">{navItems.map(n=><Link className="text-ink/75 hover:text-pine" key={n.href} href={n.href}>{n.label}</Link>)}</nav><button aria-label="打开菜单" className="ml-auto rounded-full p-2 md:hidden" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div><div className="mx-auto hidden max-w-7xl px-4 pb-3 lg:block"><SearchBox/></div>{open&&<div className="border-t border-pine/10 bg-rice p-4 md:hidden"><div className="mb-4"><SearchBox/></div><nav className="grid gap-2">{navItems.map(n=><Link onClick={()=>setOpen(false)} className="rounded-xl px-3 py-3 font-medium hover:bg-mint" key={n.href} href={n.href}>{n.label}</Link>)}</nav></div>}</header>}
